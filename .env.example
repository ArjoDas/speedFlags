# Generate with: .venv-modernise/bin/python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Required in local .env and Vercel environment variables. Never put this in VITE_*.
# For rotation: new key first, previous key second, separated by a comma.
SPEEDFLAGS_KEYS=
